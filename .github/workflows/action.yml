name: InfluxDB Test

on:
  push:
    branches:
      - main

jobs:
  test-win-influxdb:
    runs-on: windows-latest
    steps:
    - name: Use the secret
      run: python hello.py #echo ${{ secrets.INFLUXDB_URL }}
